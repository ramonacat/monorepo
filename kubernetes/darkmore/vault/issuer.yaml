apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: vault-self-issuer
  namespace: vault
spec:
  vault:
    auth:
      kubernetes:
        role: cert-manager
        serviceAccountRef:
          name: vault-issuer
    path: pki-internal/sign/internal
    server: https://vault.internal.ramona.fun:8200/
