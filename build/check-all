#/usr/bin/env bash
set -euo pipefail

pushd terraform

tflint --init
terraform init -input=false

tflint
terraform validate

popd

kubeconform -schema-location default -schema-location 'https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json' \
	-skip 'ClusterConfiguration,KubeletConfiguration,KubeProxyConfiguration,Kustomization' \
	kubernetes/
nix flake check
